name: Manual Build
on:
  pull_request:
    branches: ["main", "aaa"]
  workflow_dispatch:


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy build to target
        run: echo "asas"
